# Keyless replay keeps the LSP composition intact and replaces only the model adapter.
- id: llm-deepseek
  name: '@buddhilive/dsh-llm-deepseek'
  disabled: true

- insert:
    - id: lsp
      name: '@buddhilive/dsh-lsp'
    - id: lsp-stdio
      name: '@buddhilive/dsh-lsp-stdio'
      config:
        servers:
          fixture:
            command: !!js process.execPath
            args: ['./lsp-server.mjs']
            extensionToLanguage:
              '.ts': typescript
    - id: tool-lsp
      name: '@buddhilive/dsh-tool-lsp'
      config:
        maxLocations: 1
    - id: llm-replay
      name: '@buddhilive/dsh-llm-replay'
      config:
        providers:
          - id: deepseek-official
            name: DeepSeek
            models:
              - id: deepseek-v4-flash
              - id: deepseek-v4-pro

- id: timeout-policy
  name: '@buddhilive/dsh-tool-call-timeout-policy'
