# Opt-in Schedule patch over the shipped Web composition. The owner observes
# only roots published after this overlay loads.

- insert:
    - id: time-context
      name: '@buddhilive/dsh-time-context'

    - id: schedule
      name: '@buddhilive/dsh-schedule'

- id: ui-schedule
  disabled: false
